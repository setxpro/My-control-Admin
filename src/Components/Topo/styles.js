import styled from 'styled-components';

export const Container = styled.div`
    height: 60px;
    width: 100%;
    background-color: #00B4D8;
    color: #fff;
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0 3px;
    font-size: 1.3rem;

    h5 {
        font-size: 1.3rem;
    }

    .btn {
        cursor: pointer;
    }
    .log {
        font-size: 2rem;
    }
    .theme {
        font-size: 1.5rem;
        cursor: pointer;
    }
   
`;
